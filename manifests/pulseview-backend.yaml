apiVersion: v1
kind: Service
metadata:
  name: pulseview-backend
  labels:
    app: pulseview-backend
spec:
  selector:
    app: pulseview-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
